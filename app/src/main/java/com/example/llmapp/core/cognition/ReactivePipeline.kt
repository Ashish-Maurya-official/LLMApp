package com.example.llmapp.core.cognition

/**
 * A fast, low-latency pipeline for simple conversational turns
 * (e.g., greetings, confirmations, small talk).
 */
class ReactivePipeline {
    fun process(query: String) {
        // TODO: Fast response generation without deep search/memory
    }
}
